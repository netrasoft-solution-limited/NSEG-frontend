import type { ComponentType, SVGProps } from 'react';

/** Shape of a lucide-react icon component, safe to use as a type annotation. */
export type IconComponent = ComponentType<SVGProps<SVGSVGElement>>;